import { domMax } from "framer-motion";

/* Features de animación de framer-motion, cargadas en diferido por
   <LazyMotion> (ver main.tsx). domMax incluye layout animations,
   que usamos en el Header (layoutId) y Portafolio (popLayout). */
export default domMax;
