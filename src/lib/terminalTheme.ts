/* Colores del prompt powerlevel10k real del usuario (ver terminal.png).
   Cita visual fuera de la paleta, igual que los semáforos de macOS:
   rosa=usuario, naranja=ruta, amarillo=rama git, lavanda=hora, ❯ verde. */

export const TERMINAL_COLORS = {
  user: "#ed8296",
  path: "#f2a875",
  branch: "#ecd08b",
  time: "#aeb2ee",
  prompt: "#62c46f",
  ink: "#221436",
} as const;
